nelmio_api_doc:
    documentation:
        info:
            title: Alumni Platform API
            description: API for the INSEAD Alumni Platform
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc$)